{
  "name": "gas-boy",
  "version": "0.1.0",
  "private": true,
  "description": "Cron-based Cloudflare Worker that calls VolumeRegistry.trigger()",
  "scripts": {
    "dev": "./scripts/start-dev.sh",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250426.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "viem": "^2.21.0"
  }
}
